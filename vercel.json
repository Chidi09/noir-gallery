{
  "buildCommand": "prisma generate && prisma migrate deploy && npx tsx prisma/seed.ts && nuxt build",
  "installCommand": "pnpm install",
  "framework": "nuxtjs",
  "env": {
    "NUXT_TELEMETRY_DISABLED": "1"
  }
}
